package com.kh.switchEx;
/*
Switch-Case 
	어떤 값이 switch 안으로 주어지고 주어진 값 중에서 일치하는 값이 case 에 있다면
	case 에서 어떤 특정 행동을 진행할 수 있도록 설정
	그런데 주어진 값이 일치하는 case 가 없다면 default 로 이동하게 됨
	
	case 는 break 가 없으면 나머지 밑에 있는 case 와 default 에 작성된 코드들이 모두 실행이 되기 때문에
	case 에다가 필요한 기능을 작성 후 break; 를 작성해주는 것이 좋음
	
	default 는 필수 작성 X
	
	switch(변수 또는 표현식) {
		case 값1 :
			변수가 값1 일 때 실행될 코드 작성
			break;    필수가 아님
		case 값2 : case 값3 :
			변수가 값2 와 값3 중에 있을 경우 실행될 코드 작성
			break;
		default :
			모든 case 에 해당하는 내용이 없을 때 동작하는 공간
			break;
	}


 */
public class Switch문 {

}
